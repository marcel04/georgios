[project]
name = "georgios-backend"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "alembic>=1.16.5",
    "fastapi==0.128.0",
    "psycopg[binary]>=3.2.13",
    "pydantic-settings>=2.11.0",
    "sqlalchemy>=2.0.52",
    "uvicorn==0.39.0",
]

[dependency-groups]
dev = [
    "fastapi-cli",
    "httpx==0.28.1",
    "pytest==8.4.2",
    "ruff==0.16.6",
]

[tool.ruff]
target-version = "py39"
line-length = 88

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
